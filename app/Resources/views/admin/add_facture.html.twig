{% extends 'admin/base.html.twig' %}

{% block content %}
    <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">Nouvelle facture</div>
                    <div class="panel-body">
                        <form class="form-horizontal" action="" method="post">
                            <fieldset>
                                <div class="form-group">
                                    <label class="col-md-3 control-label" for="name">Livreur</label>
                                    <div class="col-md-9">
                                        <select name="id_livreur">
                                            {% for livreur in livreurs %}
                                            <option value="{{ livreur.id }}">{{ livreur.prenom ~" "~ livreur.nom }}</option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                </div>
                            
                                
                                <div class="form-group">
                                    <div class="col-md-12 widget-right">
                                        <button type="submit" class="btn btn-default btn-md pull-right">Mettre à jour</button>
                                    </div>
                                </div>
                            </fieldset>
                        </form>
                    </div>
                </div>
            </div>
        </div><!--/.row-->  
{% endblock %}
